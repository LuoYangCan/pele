{
  "_comment": "Pele-owned Claude hook entries. The installer merges only entries with _pele_id and preserves every other hook.",
  "hooks": {
    "PreToolUse": [
      {
        "_pele_id": "protected-branch",
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "@HARNESS_ROOT@/core/hooks/protected-branch.sh"
          }
        ]
      }
    ]
  }
}
